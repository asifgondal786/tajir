# Frontend runtime env (loaded by flutter_dotenv in debug/dev)
# For production prefer --dart-define flags instead of .env files.

# API endpoint (HTTPS in production)
API_BASE_URL=https://api.your-domain.com
